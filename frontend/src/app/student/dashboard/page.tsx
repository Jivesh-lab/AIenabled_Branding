/**
 * /student/dashboard — route entry (thin re-export).
 * Dashboard content components will be added in subsequent steps.
 */
export default function StudentDashboardPage() {
  return (
    <div className="flex-1 p-6 sm:p-8">
      {/* Dashboard content — to be built in the next steps */}
      <p className="text-sm text-slate-400">Dashboard content coming soon.</p>
    </div>
  );
}
